@page
@model Frontend.Pages.Categoria.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Categoria[0].Detalle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Categoria) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Detalle)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Idcategoria">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Idcategoria">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Idcategoria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

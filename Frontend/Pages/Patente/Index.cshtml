@page
@model Frontend.Pages.Patente.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Patente[0].nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Patente) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idPatente">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.idPatente">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.idPatente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

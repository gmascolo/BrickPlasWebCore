@page
@model Frontend.Pages.Pedido.Detalle.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DetallePedido[0].Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DetallePedido[0].Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DetallePedido[0].Subtotal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DetallePedido) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subtotal)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdDetallePedido">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdDetallePedido">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdDetallePedido">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

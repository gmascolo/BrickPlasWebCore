@page
@model Frontend.Pages.Pedido.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].Subtotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].FechaEntrega)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Pedido) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subtotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaEntrega)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdPedido">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdPedido">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdPedido">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
